# Reports

Generated charts and evaluation figures live in `reports/figures/`.

Run `python -m src.train` to regenerate the main evaluation figure.

The main figure currently used in the README is:

```text
reports/figures/confusion_matrix.png
```
