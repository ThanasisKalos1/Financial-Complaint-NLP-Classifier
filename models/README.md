# Models

Trained model artifacts are generated locally and are not committed to Git.

Run either `python -m src.train` or `notebooks/Modeling.ipynb` to recreate:

```text
models/complaint_classifier.joblib
```
